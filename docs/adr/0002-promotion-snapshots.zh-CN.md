# 晋升使用不可变源快照

[English](0002-promotion-snapshots.md) · [文档索引](../README.zh-CN.md)

状态：已接受

晋升会记录不可变的源快照，并创建复用已验证内容寻址字节的目标元数据；它不会移动
或覆盖源制品，也不会同步复制字节。这样可以让 Worker 保持可重试和幂等，明确源与
目标的授权边界，并由后续复制流程通过检查点和完整性校验负责跨对象存储的持久字节传输。
