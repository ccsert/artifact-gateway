# 候选验收记录：cb59b227

[English](2026-09-08-cb59b227.md) | [文档索引](../README.zh-CN.md)

本轮把 Maven/Raw Group 来源追溯和新版目录树纳入候选版本。源码、发行包和 GHCR 发布镜像分别完成隔离升级、回滚、前滚与恢复；发布的 Console 镜像也完成登录和恢复后的 Group 目录验收。[#26](https://github.com/ccsert/artifact-gateway/issues/26) 的候选验收已完成。本记录不代表正式版本发布或生产部署。

## 固定候选

| 项目 | 值 |
| --- | --- |
| 候选版本 | `0.1.0-main.cb59b2277487` |
| 候选提交 | `cb59b227748744de705694ff50a257414a6eb199` |
| 合并记录 | [PR #34](https://github.com/ccsert/artifact-gateway/pull/34)，2026-09-08 02:50:45 UTC |
| 上一正式版本 | `v0.1.0`，提交 `cf4635f89d1038b16a491f42393f3ba7997e38a9` |
| 主线 CI | [34181463543](https://github.com/ccsert/artifact-gateway/actions/runs/34181463543)，6 项任务全部成功 |
| CI 发行物 | `artifact-gateway-0.1.0-main.cb59b2277487`，Artifact ID `10039268673` |
| 本地环境 | Docker Desktop，Linux/arm64 发行包运行容器及 GHCR 发布镜像，隔离 PostgreSQL/RustFS 数据卷 |
| 操作员 | 本机 Codex |

完整镜像引用、发行包校验和、116 份迁移校验和、演练脚本校验和、逐项日志摘要与 UTC 输出窗口见[结构化证据](2026-09-08-cb59b227.evidence.json)。主线 CI 的 Linux/amd64 测试与本机 Linux/arm64 发行包验收分别记录，不能互相替代。

## 已完成的验收

| 验收 | 结果与范围 |
| --- | --- |
| 主线代码与客户端 | CI 通过契约、集成、质量、Console，以及 OCI、Raw、Maven/Gradle、npm、PyPI、Go、APT、Conan 原生回归；另包含 Cargo 契约和 APT signer rotation。 |
| 发行包 | CI 产出的 6 个归档全部通过 `SHA256SUMS`；上一正式版本 Linux/arm64 归档也通过官方校验和。实际执行的两个 Gateway 二进制均返回预期版本和完整 SHA。 |
| 迁移 | 基线 114 份迁移，候选 116 份；仅新增 `000114_managed_console_themes.sql` 与 `000115_proxy_browse_index.sql`。发行包迁移字节与固定提交逐份一致，运行数据库确认 116 条迁移记录及两项新增校验和。 |
| 源码升级与恢复 | 使用 `v0.1.0` 的完整 SHA 分别完成升级、二进制回滚、前滚，以及备份恢复。 |
| 发行包升级与恢复 | 使用校验过的发行包组装本地运行容器，再以固定 image ID 完整重跑上述演练；没有重新编译其中的 Gateway 二进制。 |
| 发布镜像升级与恢复 | 成功拉取并固定基线、候选 Gateway 和候选 Console 的 GHCR manifest digest，核对镜像标签及实际 Gateway 二进制身份。直接使用发布镜像重跑升级、二进制回滚、前滚和完整备份恢复，全部通过。 |
| 恢复内容 | 保留 OCI/Maven/Raw/Conan/Go 制品、Go 三种表示及恢复意图、晋级与复制记录、已完成复制校验、隔离状态、Grant 与审计；备份后的新增内容被移除。 |
| Group 专项恢复 | 备份前创建两个同路径、不同摘要的 Raw 来源和显式 reader Grant；备份后改变成员顺序、撤销 Grant 并新增文件。恢复后验证原顺序、两个摘要、授权允许/拒绝、匿名拒绝及新增文件消失。 |
| 实际 Console | CI 的 Console 发行包及 GHCR 发布镜像分别通过真实令牌登录、Nginx API 转发和恢复后的 Group 双来源目录检查。发布镜像在 1440×1000 与 390×844 视口均显示来源及内容冲突，无横向溢出、console error/warning；Gateway `/readyz`、Console `/console-healthz` 及经 Console 转发的 `/readyz` 均为 204。CI 另覆盖主题用例。 |

本地发行包运行容器与 GHCR 发布镜像分别记录。前者使用校验过的发行包和按仓库 Dockerfile digest 固定的运行基座；后者直接从 GHCR 拉取并运行，固定引用如下。两类本地 image ID 均见证据 JSON。

- 基线 Gateway：`ghcr.io/ccsert/artifact-gateway@sha256:c8155974434e708f1aad5ea683e6b52d45bc3c8b256b4ed5f072f5d4fca5a993`
- Gateway：`ghcr.io/ccsert/artifact-gateway@sha256:22f1a0761ab457855ef43c35c35ff5edaca6e97e795e1f1a18bc8819d7e88df0`
- Console：`ghcr.io/ccsert/artifact-gateway-console@sha256:38c3b3a6270119d342ce532b9fd4d50fd9f5578c2acc85fed4e4d94c6181d376`

源码升级用例验证的是单 Proxy Maven 缓存和 Go Proxy 离线读取。新 Maven 策略仅在单候选时复用无 `resolution_scope` 的旧缓存；多个候选或范围变化可能需要重新解析和获取上游，Group 目录也只接纳当前范围的缓存证据。因此，本记录不承诺所有旧多成员 Group 在断网升级后都能沿用旧缓存。

## 验收边界与后续

早期匿名 token 请求返回 401，现有 GitHub OAuth 拉取基线被拒绝，候选 digest 拉取返回 `manifest unknown`。使用本机已配置的 GHCR 登录重试后，三个发布镜像全部成功拉取；此前访问阻断已解除。历史失败与重试成功分别保留日志摘要。

发布镜像升级/回滚/前滚于 03:53:22–03:54:03 UTC 通过，完整备份恢复于 03:53:23–03:53:46 UTC 通过，供浏览器检查的 Group 变更/恢复于 03:54:21–03:54:25 UTC 通过。运行中的 Gateway 与 Console image ID 均匹配已拉取的候选镜像。原开发服务 `/readyz` 仍为 204；所有演练使用独立项目及数据卷。

本轮没有待解决的候选验收阻断。正式 release tag、生产部署及目标环境验收仍需单独记录；原生客户端 CI、Linux/arm64 本地验收和生产放行不能互相替代。

## 复跑入口与证据保存

遵循[受控部署就绪](../release-readiness.zh-CN.md)的镜像参数约定。固定候选提交和版本，使用独立测试凭据：

```sh
export GATEWAY_UPGRADE_FROM_REF=cf4635f89d1038b16a491f42393f3ba7997e38a9
export GATEWAY_READINESS_REF=cb59b227748744de705694ff50a257414a6eb199
export GATEWAY_READINESS_VERSION=0.1.0-main.cb59b2277487
export GATEWAY_UPGRADE_FROM_IMAGE=ghcr.io/ccsert/artifact-gateway@sha256:c8155974434e708f1aad5ea683e6b52d45bc3c8b256b4ed5f072f5d4fca5a993
export GATEWAY_READINESS_IMAGE=ghcr.io/ccsert/artifact-gateway@sha256:22f1a0761ab457855ef43c35c35ff5edaca6e97e795e1f1a18bc8819d7e88df0
make upgrade-readiness
make backup-restore-readiness
```

运行前通过环境变量设置 `GATEWAY_ENV_FILE` 和所选择的固定镜像。未提供镜像参数时执行源码模式，必须按源码证据记录。`group-recovery-fixture.py` 已纳入恢复脚本，可复跑成员、来源和 Grant 的变更/恢复用例。

本地详细日志保存在工作区 `output/candidate-validation/cb59b227`，未提交；对应 SHA-256 与 UTC 输出窗口已写入证据 JSON。临时 GHCR 登录配置已移除，凭据不进入本记录。回滚保持数据库仅前向迁移；二进制回滚和 PostgreSQL/RustFS 成对恢复按[恢复手册](../recovery-runbook.zh-CN.md)执行。
