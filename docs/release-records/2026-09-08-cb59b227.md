# Candidate Acceptance Record: cb59b227

[简体中文](2026-09-08-cb59b227.zh-CN.md) | [Documentation index](../README.md)

This candidate includes Maven/Raw Group provenance and the revised directory tree. Source, distribution archives, and published GHCR images separately passed isolated upgrade, rollback, forward recovery, and restore checks. The published Console image also passed login and restored Group directory validation, completing candidate acceptance for [#26](https://github.com/ccsert/artifact-gateway/issues/26). This record is not a formal release or production deployment.

## Frozen Candidate

| Field | Value |
| --- | --- |
| Candidate version | `0.1.0-main.cb59b2277487` |
| Candidate revision | `cb59b227748744de705694ff50a257414a6eb199` |
| Merge | [PR #34](https://github.com/ccsert/artifact-gateway/pull/34), 2026-09-08 02:50:45 UTC |
| Previous formal release | `v0.1.0`, revision `cf4635f89d1038b16a491f42393f3ba7997e38a9` |
| Mainline CI | [34181463543](https://github.com/ccsert/artifact-gateway/actions/runs/34181463543), all 6 jobs succeeded |
| CI distribution | `artifact-gateway-0.1.0-main.cb59b2277487`, artifact ID `10039268673` |
| Local environment | Docker Desktop, Linux/arm64 distribution runners and published GHCR images, isolated PostgreSQL/RustFS volumes |
| Operator | Codex on the local workstation |

[Structured evidence](2026-09-08-cb59b227.evidence.json) contains full image references, archive checksums, all 116 migration checksums, harness checksums, log digests, and per-gate UTC output windows. Mainline Linux/amd64 CI and local Linux/arm64 distribution rehearsals are separate evidence.

## Completed Validation

| Gate | Result and scope |
| --- | --- |
| Mainline code and clients | CI passed contract, integration, quality, Console, OCI, Raw, Maven/Gradle, npm, PyPI, Go, APT, and Conan regressions, plus Cargo contracts and APT signer rotation. |
| Distributions | All 6 CI archives passed `SHA256SUMS`; the previous formal Linux/arm64 archive also matched its official checksum. Both executed Gateway binaries reported the expected version and full revision. |
| Migrations | Baseline 114 files, candidate 116; only `000114_managed_console_themes.sql` and `000115_proxy_browse_index.sql` were added. Every packaged migration matched the fixed revision byte-for-byte; the running database confirmed 116 records and both new checksums. |
| Source upgrade and restore | The full `v0.1.0` revision passed upgrade, binary rollback, forward recovery, and backup/restore rehearsals. |
| Distribution upgrade and restore | Repeated those rehearsals using local containers assembled from checksum-verified archives and pinned by image ID. The Gateway binaries were not rebuilt. |
| Published-image upgrade and restore | Pulled and pinned GHCR manifest digests for the baseline, candidate Gateway, and candidate Console; checked labels and actual Gateway binary identities. The published images passed upgrade, binary rollback, forward recovery, and complete backup/restore rehearsals. |
| Restored content | Preserved OCI/Maven/Raw/Conan/Go artifacts, all three Go representations and recovery intents, promotion/replication records and verified completed checkpoints, quarantine, grants, and audit evidence; post-backup additions disappeared. |
| Group recovery | Seeded two conflicting Raw sources and explicit reader grants, then reordered members, revoked a grant, and published another file after backup. Restore recovered original order, both digests, reader allow/deny, anonymous denial, and removal of the new file. |
| Live Console | Both the CI Console archive and published GHCR image passed token login, Nginx API forwarding, and restored Group provenance checks. The published image displayed both sources and the conflict indication at 1440×1000 and 390×844, with no horizontal overflow or console errors/warnings. Gateway `/readyz`, Console `/console-healthz`, and proxied `/readyz` returned 204. CI separately covered themes. |

Local distribution runners and published GHCR images are recorded separately. The runners use verified archive payloads with runtime bases pinned to the repository Dockerfile digests. Published images were pulled directly from GHCR and executed using the following fixed references. Local image IDs for both categories appear in the evidence JSON.

- Baseline Gateway: `ghcr.io/ccsert/artifact-gateway@sha256:c8155974434e708f1aad5ea683e6b52d45bc3c8b256b4ed5f072f5d4fca5a993`
- Gateway: `ghcr.io/ccsert/artifact-gateway@sha256:22f1a0761ab457855ef43c35c35ff5edaca6e97e795e1f1a18bc8819d7e88df0`
- Console: `ghcr.io/ccsert/artifact-gateway-console@sha256:38c3b3a6270119d342ce532b9fd4d50fd9f5578c2acc85fed4e4d94c6181d376`

The source upgrade fixture verifies a single-Proxy Maven cache and offline Go Proxy reads. The new Maven policy reuses legacy cache without `resolution_scope` only for a single candidate; multiple candidates or scope changes may require upstream resolution and retrieval, and Group directories admit only current-scope cache evidence. This record therefore does not promise reuse of every legacy multi-member Group cache during an offline upgrade.

## Acceptance Boundaries and Follow-up

Initial anonymous token requests returned 401, the baseline pull using existing GitHub OAuth credentials was denied, and candidate digest pulls returned `manifest unknown`. Retrying with the workstation's configured GHCR login successfully retrieved all three published images, resolving the access blocker. Log digests preserve both the earlier failures and successful retries.

Published-image upgrade/rollback/forward recovery passed at 03:53:22–03:54:03 UTC, complete backup/restore at 03:53:23–03:53:46 UTC, and the browser fixture's Group mutation/restore at 03:54:21–03:54:25 UTC. Running Gateway and Console image IDs matched the retrieved candidate images. The existing development service still returned 204 from `/readyz`; rehearsals used separate projects and volumes.

No candidate acceptance blocker remains. Formal release tags, production deployment, and target-environment validation must be recorded separately; native-client CI, local Linux/arm64 acceptance, and production approval are distinct evidence.

## Reproduction and Evidence Storage

Follow the image parameters in [release readiness](../release-readiness.md). Pin the candidate revision/version and use isolated test credentials:

```sh
export GATEWAY_UPGRADE_FROM_REF=cf4635f89d1038b16a491f42393f3ba7997e38a9
export GATEWAY_READINESS_REF=cb59b227748744de705694ff50a257414a6eb199
export GATEWAY_READINESS_VERSION=0.1.0-main.cb59b2277487
export GATEWAY_UPGRADE_FROM_IMAGE=ghcr.io/ccsert/artifact-gateway@sha256:c8155974434e708f1aad5ea683e6b52d45bc3c8b256b4ed5f072f5d4fca5a993
export GATEWAY_READINESS_IMAGE=ghcr.io/ccsert/artifact-gateway@sha256:22f1a0761ab457855ef43c35c35ff5edaca6e97e795e1f1a18bc8819d7e88df0
make upgrade-readiness
make backup-restore-readiness
```

Set `GATEWAY_ENV_FILE` and the selected pinned images through the environment before running. Without image options, the scripts run in source mode and must be recorded as source evidence. The restore script includes `group-recovery-fixture.py`, making member, provenance, and grant mutation/recovery repeatable.

Detailed local logs remain in workspace `output/candidate-validation/cb59b227` and are not committed; SHA-256 values and UTC output windows are recorded in the JSON. Temporary GHCR login configuration was removed, and credentials are absent from this record. Keep schema changes forward-only; binary rollback and paired PostgreSQL/RustFS restoration follow the [recovery runbook](../recovery-runbook.md).
