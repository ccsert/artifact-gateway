---
repositories_repositoryId_replications:
  parameters:
  - "$ref": "../components/parameters.yaml#/RepositoryId"
  post:
    operationId: createRepositoryReplication
    parameters:
    - "$ref": "../components/parameters.yaml#/IdempotencyKey"
    requestBody:
      required: true
      content:
        application/json:
          schema:
            "$ref": "../components/schemas.yaml#/ReplicationRequest"
    responses:
      '202':
        description: Checkpointed replication plan accepted
        content:
          application/json:
            schema:
              "$ref": "../components/schemas.yaml#/ReplicationPlan"
      '400': {"$ref": "../components/responses.yaml#/Problem"}
      '403': {"$ref": "../components/responses.yaml#/Problem"}
      '409': {"$ref": "../components/responses.yaml#/Problem"}
  get:
    operationId: listRepositoryReplications
    responses:
      '200':
        description: Replication plans involving this repository
        content:
          application/json:
            schema:
              type: array
              items:
                "$ref": "../components/schemas.yaml#/ReplicationPlan"
      '403': {"$ref": "../components/responses.yaml#/Problem"}
repositories_repositoryId_replications_replicationPlanId:
  parameters:
  - "$ref": "../components/parameters.yaml#/RepositoryId"
  - "$ref": "../components/parameters.yaml#/ReplicationPlanId"
  get:
    operationId: getRepositoryReplication
    responses:
      '200':
        description: Replication plan and checkpoint progress for this repository
        content:
          application/json:
            schema:
              "$ref": "../components/schemas.yaml#/ReplicationPlanDetail"
      '403': {"$ref": "../components/responses.yaml#/Problem"}
      '404': {"$ref": "../components/responses.yaml#/Problem"}
  delete:
    operationId: deleteRepositoryReplication
    summary: Cancel a pending or failed replication plan
    description: |
      Stops a pending or failed replication plan from being claimed again. A
      running plan cannot be cancelled because the worker owns it mid-flight;
      such a request fails with 409. Completed and already-cancelled plans are
      also not cancellable. The plan record is retained for audit.
    responses:
      '204':
        description: Replication plan cancelled
      '403': {"$ref": "../components/responses.yaml#/Problem"}
      '404': {"$ref": "../components/responses.yaml#/Problem"}
      '409': {"$ref": "../components/responses.yaml#/Problem"}
